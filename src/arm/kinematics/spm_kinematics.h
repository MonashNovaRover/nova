// Placeholder so I can check build errors

#include <vector>


class SpmKinematics
{
    public:
    
    std::vector<double> spm_fk (std::vector<double>);
    std::vector<double> spm_ik (std::vector<double>);
    SpmKinematics(){};
};