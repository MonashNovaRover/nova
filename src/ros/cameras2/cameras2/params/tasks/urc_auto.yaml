camera_streamer:
  ros__parameters:
    defaults:
      autostart: false

    cameras:
      mast_forward:
        autostart: true
        profile: snail
      mast_belly:
        autostart: true
        profile: snail
