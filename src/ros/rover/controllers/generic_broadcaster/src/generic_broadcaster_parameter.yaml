generic_broadcaster:
  publish_rate: {
    type: double,
    default_value: 50.0, # Hz
    description: "Publishing rate (Hz) of the odometry and TF messages.",
  }
  state_interface_handle: {
    type: string,
    default_value: "test/value",
    description: "The handle to use for the state interface",
  }
