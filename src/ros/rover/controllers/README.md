# controllers

This directory contains all the custom `ros2_control` controllers we use. Their configuration parameters are set in the bringup packages (`auto_bringup/params/controllers.yaml` for now).

Please consult the `ros2_control` [documentation](https://control.ros.org/rolling/doc/ros2_controllers/doc/writing_new_controller.html) to learn how to create a new controller.
