# Defines the wheel odometry data for virtual wheels on the left and right side of the rover
# Assumes T265 coordinate system with x right, y up, z backwards

# m/s for left side wheels averaged
geometry_msgs/Vector3 left_wheel_vel

# m/s for right side wheels averaged
geometry_msgs/Vector3 right_wheel_vel
