# hardware_interfaces

This is a directory containing code for the BLCMDs, such as the the custom blcmd_hardware interfaces we use for the rover. Hardware interfaces convert ROS2 commands into commands specific to the vehicle's hardware, and vice versa.
