# hardware_interfaces

This is a directory containing code for the CMDs, such as the the custom cmd_hardware interfaces we use for the rover. Hardware interfaces convert ROS2 commands into commands specific to the vehicle's hardware, and vice versa.
