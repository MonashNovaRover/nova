# nova_costmap_2d

This is a ROS2 package containing custom Costmap 2D plugins. They are then set in `auto_bringup/params/nav2_params.yaml` as the costmap layers used to compute the global/local costmaps.
