jetson$ drive
jetson$ ros2 run cameras service
jetson$ ros2 run control blcmd_status_monitor.py
jetson$ dmesg -w

metabox$ ros2 run cameras client chassis
metabox$ base
metabox$ ros2 bag record /T265/pose /depth_camera/d435_1/cloud /object_detector/markers /ar_tracker/tags -s mcap -b 500000000

jetson$ vim control/params/auto_params.yaml

metabox$ rosbridge
metabox$ foxglove-studio
jetson$ drive
jetson$ ros2 run cameras service
jetson$ ros2 run control blcmd_status_monitor.py
jetson$ dmesg -w

metabox$ ros2 run cameras client chassis
metabox$ base
metabox$ ros2 bag record /T265/pose /depth_camera/d435_1/cloud /object_detector/markers /ar_tracker/tags -s mcap -b 500000000

jetson$ vim control/params/auto_params.yaml

metabox$ rosbridge
metabox$ foxglove-studio

jetson$ auto_drive


On Screen Left:
- foxglove studio
    - Map angle and not too zoomed

On Screen Middle:
- camera feed
- rosbridge output

On Screen Right:
- autonomous output
- dmesg output
- blcmd_status_monitor.py output


Checks:
- regular drive checks
- Map not too zoomed in
- Camera visible
jetson$ auto_drive


On Screen Left:
- foxglove studio
    - Map angle and not too zoomed

On Screen Middle:
- camera feed
- rosbridge output

On Screen Right:
- autonomous output
- dmesg output
- blcmd_status_monitor.py output


Checks:
- regular drive checks
- Map not too zoomed in
- Camera visible