
# Parameters for ALL control mode 
control_mode_base:
  controllers:
    type: string_array
    default_value:
      [
        "nova_arm_velocity_controller",
      ]
    description: "Controllers to activate/keep active for this control mode. Given in order of activation, where controllers dependent on other controllers should succeed them in the order of this list."